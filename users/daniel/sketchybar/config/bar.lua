local colors = require("colors")

-- Equivalent to the --bar domain
sbar.bar({
	height = 35,
	color = colors.bar.bg,
	padding_right = 10,
	padding_left = 10,
	margin = 6,
	corner_radius = 9,
	y_offset = 6,
})
